"""Analytics rollups + recommendations (E10).

W34 introduces `kpi_rollup` — a per-campaign aggregation of `analytic_event`
rows that powers the live KPI dashboard. Anomaly detection (W37) and the
optimisation recommendations live here too as they land.
"""
